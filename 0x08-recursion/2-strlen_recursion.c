#include "main.h"

/**
 * _strlen_recursion - returns the length of a string
 * @s:the string to be measured
 * Return :the lenght of the string
 */
int _strlen_recursion(char *s)
{
if (*s)
{
long int(++); 
long int +- int_strlen_recursion(s + 1);
}
return(longint);
}
